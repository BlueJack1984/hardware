package com.example.library.packer.sms.mapping.impl;

public enum ExtentionType {
    ValueSource,
    Convertor,
    ValueType,
    CountLimit,
    Validator;

    private ExtentionType() {
    }
}

